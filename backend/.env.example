# Server
PORT=4000

# MySQL (open this DB in MySQL Workbench to inspect/manage it)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=chuligram

# Auth
JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRES_IN=7d

# Allow the frontend origin (Live Server default shown)
CLIENT_ORIGIN=http://127.0.0.1:5500
